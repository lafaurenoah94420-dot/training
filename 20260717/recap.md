# 2026-07-17

## Exos
notions : concaténation, conditions if/elseif/else, for ipairs, fonction return, for+if
score : 5/5
bloqué : —
notes : Première session Lua. Solide sur la logique ; freins syntaxe (guillemets vs variables, else then, ipairs, return du résultat, x vs table dans le if). 5/5 avec indices.

## Projet
concept : Fonctions
exercices : 6/6
bloqué : —
notes : Bien progressé sur function/return/params. Pièges récurrents : math.min/max avec un seul argument, then oublié sur elseif. Fichier ciblé parfois confondu au début.

## Libre
sujet : Nahla — Verdict croquettes
notions utilisées : io.read, tonumber, if/elseif, function, print, concaténation ..
features : message avec nombre de bols via ..
notes : Confusion initiale Python vs Lua (input/int) et appel de fonction hors du end ; programme OK + feature concat
